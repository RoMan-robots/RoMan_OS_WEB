const uefiButton = document.getElementsByClassName("uefi-button");

function runFromUEFI(){
    const bootSelect = document.querySelector(".select-boot").value;
    const bootChoise = bootSelect.value;
}